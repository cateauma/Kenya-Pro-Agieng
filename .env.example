# Get these from Supabase Dashboard: Project Settings → API
# Project URL (from your DB host: https://<project-ref>.supabase.co)
VITE_SUPABASE_URL=https://cmfdcuxqrjkzcrkamhrx.supabase.co
# Anon (public) key - safe for client-side
VITE_SUPABASE_ANON_KEY=your_anon_key_here

# Backend API (optional). If set, login/register use the Node backend and kpao DB.
# Run the backend with: cd backend && npm run dev
VITE_API_URL=http://localhost:4000
 